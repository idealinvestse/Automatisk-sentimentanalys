feat(webui): deprecate NiceGUI + add Executive Insights tab (Spår B.3, C.2)

B.3 — Archive decision for NiceGUI:
- Add app/archive/README.md with deprecation banner pointing to webui/
- Update AGENT_CONTEXT.md, PROJECT_STATUS.md, docs/ROADMAP.md, CHANGELOG.md
  to mark webui as primary dashboard and NiceGUI as legacy/reference only

C.2 — Executive Insights tab on top of real data:
- New /executive route with aggregated KPIs across all demo calls:
  total calls, avg QA, avg sentiment, total alerts, critical calls, LLM cost
- Risk overview (churn/escalation/satisfaction) with risk-level distribution
- Top alert rules, category breakdown, agent benchmark table
- All data aggregated client-side from useDemoReports via
  aggregateExecutiveSummary() — no new backend endpoint required
- Add Briefcase nav icon + e2e smoke test for /executive

Verified: npm run lint (0 errors), npm run build (clean, /executive route
generated), Playwright smoke tests pass for all routes including /executive.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
