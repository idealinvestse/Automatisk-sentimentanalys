feat(webui): virtualize transcript view with @tanstack/react-virtual (Spår B.4)

- New TranscriptView component (webui/src/components/transcript-view.tsx)
  using useVirtualizer from @tanstack/react-virtual
- Renders only visible rows + overscan (8) regardless of transcript length
- Dynamic row measurement via measureElement handles varying text heights
- /calls/[id] now uses TranscriptView instead of inline non-virtualized list
- Shows "Virtualiserad vy · N segment" description for transcripts > 50 turns
- Prepares webui for real (long) production calls beyond demo data

Verified: npm run lint (0 errors, 2 pre-existing TanStack warnings),
npm run build (clean), Playwright smoke tests pass for all routes.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
