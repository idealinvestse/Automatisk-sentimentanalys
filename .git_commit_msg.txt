fix: address all production checklist gaps (D.3 — PII, backup, docker, GPU docs)

Gap 1 — PII default (production safety):
- Changed anonymize_before_llm from False to True for callcenter profile
  (src/profiles.py:166). PII is now redacted before both local analysis
  and LLM calls by default — privacy-by-design for production.
- Verified: 72 pipeline/PII/Groq/LLM tests pass + 7 golden tests pass.

Gap 2 — Environment variables in deployment:
- Updated docker-compose.webui.yml with env_file: .env for API service
- Added api_cache volume for .cache directory
- Added API_CORS_ORIGINS env var to ensure webui can reach API

Gap 3 — GPU verification documentation:
- Added 6-step GPU verification guide to PRODUCTION_CHECKLIST.md
  (build, CUDA check, run with env_file, sentiment test, ASR test, nvidia-smi)

Gap 4 — Automated backup:
- New scripts/backup.py: timestamped tar.gz archives with rotation
- Supports --redis flag for BGSAVE before archiving
- Cron example documented in PRODUCTION_CHECKLIST.md

Updated docs/PRODUCTION_CHECKLIST.md: 18 PASS, 1 FAIL (GPU verification
on real host — requires physical GPU). All other gaps closed.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
