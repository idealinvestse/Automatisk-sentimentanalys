feat(webui): model routing tier selector in Test Lab (Spår C.1)

- New routing-tier module (webui/src/lib/routing-tier.ts) mirroring
  src/llm/routing.py — FAST/BALANCED/DEEP tiers with model slugs,
  cost/M tokens, latency band, quality rating
- New ModelRoutingCard component with three tier buttons showing
  cost/quality tradeoffs; "auto" badge when tier is overridden by
  segment count or deep_analysis flag (same logic as select_model())
- Test Lab page (/testlab) now includes ModelRoutingCard when LLM is
  enabled with openrouter provider; sends llm_model in pipeline request
- No backend changes required — PipelineRequest.llm_model already exists

Verified: npm run lint (0 errors), npm run build (clean),
Playwright smoke tests pass for all 8 routes.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
