pandas>=2.2.0
typer>=0.12.3
rich>=13.7.0
faster-whisper>=1.0.0

# Optional backends (install as needed):
#   whisperx   (pip install whisperx) - for backend=whisperx (alignment + diarization)
#   noisereduce / torchaudio - for future Task 1.4 pre-processing
